dependencies:
  pre:
    - npm install grunt-cli bower --global
  post:
    - bower install
test:
  pre:
    - grunt build:prod
deployment:
  develop:
    branch: develop
    commands:
      - grunt deploy:develop
  production:
    branch: master
    commands:
      - grunt deploy:prod
