dependencies:
  pre:
    - npm install grunt-cli bower --global
  post:
    - bower install
deployment:
  staging:
    branch: develop
    commands:
      - grunt deploy:develop
  production:
    branch: master
    commands:
      - grunt deploy:production
