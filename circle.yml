dependencies:
  pre:
    - npm install grunt-cli bower --global
  post:
    - bower install
test:
  post:
    - npm test
deployment:
  staging:
    branch: develop
    commands:
      - grunt deploy:staging
  production:
    branch: master
    commands:
      - grunt deploy:production
